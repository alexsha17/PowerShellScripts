Skript 1 / einfach


$now = Get-Date
Write-Host "Datum: $now"
$computerName = Get-ComputerInfo
Write-Host "Computername: $computerName"
$processes = Get-Process
Write-Host "Anzahl der Prozesse: $($processes.Count)"
$files = Get-ChildItem
Write-Host "Anzahl der Dateien im Verzeichnis: $($files.Count)"
